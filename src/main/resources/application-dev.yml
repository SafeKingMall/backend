spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://board-project-mariadb.cfhzauu7smdq.ap-northeast-2.rds.amazonaws.com:3306/safeking_dev
    username: safeking_dev
    password: 1234

  batch:
    job:
      names: ${job.name:NONE}
      enabled: false
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_batch_fetch_size: 1000
        format_sql: true
    open-in-view: false
  logging.level:
    org.hibernate.SQL: debug
  redis:
    host: localhost
    port: 6379